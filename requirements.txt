# This requirements file lists all third-party dependencies for this project.
#
# Run 'pip install -r requirements.txt -t lib/' from the project root to install
# these dependencies in `lib/` subdirectory.
#
# Note: The `lib` directory is added to `sys.path` by `appengine_config.py`.
Flask==0.10
feedparser==5.1.3
requests==1.2.3
requests_oauthlib==0.4.0
tweepy==2.2